struct SettingsToSave {
    int windowX;
    int windowY;
    int windowPosX;
    int windowPosY;
    char *playingFileId; //NOTE: Stored on the per board arena
    float volumeBg;
    float volumeFg;
};